# DeepTrace — pinned dependencies
# Phase 1: all listed. Phase 2: all active.

# LangGraph / LangChain / LangSmith
langgraph>=0.2.0
langchain>=0.2.0
langchain-core>=0.2.0
langsmith>=0.1.0

# LLM SDKs (Phase 1: imported only inside USE_MOCK=false branches)
anthropic>=0.50.0
openai==1.50.0
google-genai>=1.0.0
groq==1.1.1

# Structured output
instructor==1.4.3
pydantic==2.9.0

# Neo4j
neo4j==5.26.0
networkx==3.4.0
pyvis==0.3.2

# Search
tavily-python==0.5.0

# Frontend
streamlit==1.41.0
plotly==5.24.0

# HTTP / async (httpx<0.28 required: openai 1.50 passes proxies to httpx, removed in 0.28)
httpx<0.28
aiohttp==3.11.0
tenacity>=8.4.1,<9.0.0

# PDF report
fpdf2==2.8.1
weasyprint==61.2
markdown==3.5.1

# Utilities
python-dotenv==1.0.1
rich==13.9.0
click==8.1.7

# Dev / test
pytest==8.3.0
pytest-asyncio==0.24.0
